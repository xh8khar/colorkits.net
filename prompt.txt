Create a new Next.js 16 project called {ProjectName} with output: 'export', Tailwind CSS v4 (no config file), and class-based dark mode. The layout must match jsonkits.net (reference repo: https://github.com/xh8khar/jsonkits.net) exactly. Do not use shadcn or any UI library — everything is custom.
Reference layout structure:
- AppShell — root wrapper component: renders Sidebar + Navbar + <main className="flex-1 flex flex-col min-w-0"> + Footer in a <div className="flex min-h-screen">. Manages sidebarOpen state (boolean), provides onMenuClick toggle and onMobileClose handler. Runs useEffect on pathname to close sidebar on route change.
- Sidebar — lg:sticky top-0 h-screen w-72, mobile: fixed overlay z-50 with translate-x transform. Has a header with logo + close button (lg:hidden), search input filtering tools array, category accordion sections from a hardcoded categoryGroups array. Categories: converters, pickers, palettes, gradients, accessibility, image, adjustments, reference, utility. Each category gets a colored dot (bg-blue-500, bg-amber-500, etc.). Expand/collapse toggle with chevron, shows tool count badge. Active tool gets bg-rose-50 dark:bg-rose-900/30 text-rose-600 dark:text-rose-400. Search shows flat results. Mobile backdrop overlay when open. Imports tools from @/lib/navigation and ToolCategory from @/types. Uses usePathname from next/navigation.
- Navbar — sticky top-0 z-30 with backdrop-blur. Left: hamburger button (lg:hidden) + logo (lg:hidden, colored dots + name). Center: desktop links (Blog, Learn, About, Contact) — hidden on mobile. Right: SocialShare dropdown + ThemeToggle. SocialShare: button toggles a dropdown with share links (Facebook, X/Twitter, LinkedIn, Reddit, WhatsApp) using window.open share URLs. Closes on outside click via useRef + useEffect. No mega-menus, no dropdowns for nav links.
- Footer — 4-column grid: 2 tool columns (from navItems with children, sliced to 4 links each), Company column (Privacy Policy, Terms of Service, Cookies, Disclaimer), brand column (description + copyright). Uses navItems from @/lib/navigation. Accent hover color is rose-500.
- LayoutShell — thin wrapper that returns <AppShell>{children}</AppShell>.
Theme:
- Dark mode via .dark class on <html>, toggled by localStorage('theme').
- Inline script in root layout <head> to prevent flash.
- ThemeToggle component: sun/moon icons, reads/writes localStorage, toggles class.
- Accent color: rose-500 (#f43f5e).
Config:
- config/navigation.json — JSON array of nav items: { label, href?, children? }. Editable without touching TypeScript.
- src/lib/navigation.ts — reads the JSON, exports navItems and a flat tools array derived from dropdown children with slug, name, category, description, id.
- src/types/index.ts — exports Tool, ToolCategory, NavItem types.
Tool page pattern:
- Server page.tsx with metadata export (title, description) + <ToolPageClient />.
- Client ToolPageClient.tsx with 'use client' directive, uses ToolLayout.
- Components at src/components/tools/ToolLayout.tsx, InputPanel.tsx, OutputPanel.tsx with ToolLayout being a dual-panel layout (input textarea + output + action buttons).
- Converters at src/lib/converters.ts with exported pure functions in camelCase (e.g., hexToRgb).
Project structure:
src/
├── app/
│   ├── globals.css
│   ├── layout.tsx
│   ├── page.tsx
│   └── {slug}/
│       ├── page.tsx
│       └── ToolPageClient.tsx
├── components/
│   ├── home/
│   │   ├── HomeContent.tsx
│   │   └── ToolCard.tsx
│   ├── layout/
│   │   ├── AppShell.tsx
│   │   ├── Footer.tsx
│   │   ├── LayoutShell.tsx
│   │   ├── Navbar.tsx
│   │   ├── Sidebar.tsx
│   │   └── ThemeToggle.tsx
│   └── tools/
│       ├── InputPanel.tsx
│       ├── OutputPanel.tsx
│       └── ToolLayout.tsx
├── lib/
│   ├── navigation.ts
│   └── site.ts
└── types/
    └── index.ts
Generate all boilerplate: Navbar, Sidebar, Footer, AppShell, LayoutShell, ThemeToggle, HomeContent, ToolCard, ToolLayout, InputPanel, OutputPanel, layout.tsx, globals.css, all legal/info pages (about, contact, privacy, terms, cookies, disclaimer), sitemap.ts, config/navigation.json, blog/learn structure. You can generate placeholder content for texts.
Build must pass with npm run build producing static export with no errors.